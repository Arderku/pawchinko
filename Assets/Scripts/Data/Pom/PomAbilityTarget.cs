namespace Pawchinko
{
    /// <summary>
    /// Which board(s) an ability applies to when resolved.
    /// </summary>
    public enum PomAbilityTarget
    {
        SelfBoard = 0,
        EnemyBoard = 1,
        BothBoards = 2
    }
}
